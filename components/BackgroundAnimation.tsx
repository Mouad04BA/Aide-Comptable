import React from 'react';

/**
 * DEPRECATED: This component implemented a background parallax effect with performance concerns
 * due to using a scroll event listener. It has been replaced by a more performant,
 * content-focused scroll animation system using the Intersection Observer API
 * via the ScrollAnimationWrapper component.
 */
const BackgroundAnimation: React.FC = () => {
    return null;
};

export default BackgroundAnimation;
